{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": ".claude/hooks/pre-commit-check.sh"
          },
          {
            "type": "command",
            "if": "Bash(git*push*ai-memory*)",
            "command": ".claude/hooks/pre-memory-push.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before finishing: check if FILES were modified in this session (code, configs, workflows, skills — not just .memory/). If yes, verify .memory/CHANGELOG.md and .memory/TIME_LOG.md were updated. If no files were modified (pure discussion, analysis, or research), do NOT create CHANGELOG or TIME_LOG entries — those are for actual changes only."
          }
        ]
      }
    ]
  }
}
