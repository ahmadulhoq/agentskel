# [APP_NAME] — Project Rules

> Framework rules live in `.agents/rules/` (always active).
> This file is for **project-specific overrides and context** only.

## Project Context
<!-- What this project is, its goals, key domain knowledge the agent needs.
     This section is stable — update when the project's purpose evolves. -->

## Project Rules
<!-- Ad-hoc rules specific to this project. Examples:
     - "No git support — this project uses Mercurial"
     - "Never touch the auth module without security review"
     - "All API responses must include request tracing headers" -->
