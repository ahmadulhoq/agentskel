---
name: setup-workspace
description: When setting up agentskel as a workspace dispatcher — multiple independent projects under one parent folder, each with its own git repo. Run once at the workspace root to create routing dispatcher files. Does NOT install agentskel in subdirs.
---

Read and follow the full workflow at `.agents/workflows/setup-workspace.md`.
